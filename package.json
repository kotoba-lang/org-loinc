{
  "name": "org-loinc",
  "private": true,
  "description": "LOINC code format, Mod-10 check digit, six-axis part model, and release-CSV parser in portable .cljc (no LOINC content shipped)",
  "scripts": {
    "test:cljs": "kbb --backend sci --classpath \"$(kbb -A:cljs -Spath)\" scripts/verify-cljs.cljk"
  }
}
